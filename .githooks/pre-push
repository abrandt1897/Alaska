#!/bin/sh
# Runs lint, typecheck, format check, and tests (with coverage) before
# allowing a push. Installed automatically via `npm install` (see
# package.json "prepare" script), which sets `git config core.hooksPath
# .githooks`.

set -e

npm run lint
npm run typecheck
npm run format:check
npm run test:coverage
