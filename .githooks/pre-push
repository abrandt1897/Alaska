#!/bin/sh
# Runs lint, format check, and tests before allowing a push.
# Installed automatically via `npm install` (see package.json "prepare" script),
# which sets `git config core.hooksPath .githooks`.

set -e

npm run lint
npm run format:check
npm test
